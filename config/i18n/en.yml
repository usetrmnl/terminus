en:
  api:
    display:
      show:
        invalid_api_key: Invalid API key.
    log:
      create:
        no_device: Unable to find device.
        invalid_device_id: Invalid device ID.
        invalid_payload: Validation failed due to invalid payload.
    setup:
      show:
        invalid_headers: Invalid request headers.
    shared:
      errors:
        validation: "Validation failed."
  aspects:
    designs:
      event_source:
        disconnected: Event stream disconnected.
      importer:
        imported: Imported design.
    devices:
      sensors:
        synchronizer:
          duplicate: Duplicate sensor detected. Skipped.
      synchronizer:
        invalid_api_key: Unable to find device by API key.
    extensions:
      exchanges:
        refresher:
          bad_fetch: Unable to fetch, invalid result.
      importers:
        local:
          creators:
            exchange:
              imported: Imported extension exchange.
            extension:
              imported: Imported extension.
        remote:
          creator:
            bad_transform: Unable to transform exchange template.
          transformer:
            no_serverless: Serverless transforms are not supported yet.
          transformers:
            default:
              imported: Imported from TRMNL.
    firmware:
      headers:
        parser:
          debug: Processing device request headers.
    screens:
      mold_builder:
        debug: Screen mold built.
      positioner:
        empty_playlist: Unable to obtain next screen. Playlist has no items.
    unzipper:
      no_directories: Directories are not allowed.
  date:
    formats:
      day_month_long: "%A, %B %-d"
  datetime:
    relative:
      distance_in_words:
        about_x_hours:
          one: about %{count} hour
          other: about %{count} hours
        about_x_months:
          one: about %{count} month
          other: about %{count} months
        about_x_years:
          one: about %{count} year
          other: about %{count} years
        almost_x_years:
          one: almost %{count} year
          other: almost %{count} years
        half_a_minute: half a minute
        less_than_x_minutes:
          one: less than a minute
          other: less than %{count} minutes
        less_than_x_seconds:
          one: less than %{count} second
          other: less than %{count} seconds
        over_x_years:
          one: over %{count} year
          other: over %{count} years
        x_days:
          one: "%{count} day"
          other: "%{count} days"
        x_minutes:
          one: "%{count} minute"
        x_months:
          one: "%{count} month"
          other: "%{count} months"
          other: "%{count} minutes"
        x_seconds:
          one: "%{count} second"
          other: "%{count} seconds"
      future: in %{time}
      past: "%{time} ago"
  designs:
    import:
      create:
        notice: Design imported!
  devices:
    shared:
      _fields:
        commands:
          identify: Identify
          screen_backward: Previous Screen
          screen_first: First Screen
          screen_forward: Next Screen
          screen_last: Last Screen
  dry_validation:
    errors:
      extension:
        hash?: "must be a JSON object"
      model:
        hash?: "must be a JSON object"
  extensions:
    gallery:
      index:
        alert: Unable to process TRMNL API.
    import:
      create:
        notice: Extension imported!
    update:
      notice: Changes saved.
  jobs:
    extensions:
      screen:
        info: Enqueued extension screen upsert.
        error: Unable to find extension.
    synchronizers:
      firmware:
        warning: Firmware synchronization is disabled.
      font:
        warning: Font synchronization is disabled.
      model:
        warning: Model synchronization is disabled.
  time:
    formats:
      shorter: "%-I:%M %p"
    start_of_week: 0
