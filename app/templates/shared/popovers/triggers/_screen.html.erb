<%= tag.button type: :button,
               class: locals.fetch(:class, "bit-popover-trigger"),
               interestfor: "tooltip-action-#{name}",
               popovertarget: "popover-#{name}" do %>
  <%= yield %>
<% end %>
