<%= render "shared/title", text: "Playlists" %>

<section class="bit-page page-playlists">
  <header class="bit-header">
    <h1 class="label">
      Playlists
      <%= render "shared/popovers/triggers/default", name: :overview, width: 20, height: 20 %>
    </h1>

    <div class="bit-actions">
      <%= render "shared/actions/search", path: routes.path(:playlists), query: %>
      <%= render "shared/actions/new", path: routes.path(:playlist_new) %>
    </div>
  </header>

  <%= scope(:popover_default_content, name: :overview, label: "Overview").render do %>
    <p>Playlists allow you to control what is displayed on your device by adding items (screens) to your playlist in the order you wish to view them. Every device must have a playlist -- with items -- in order to display images on your device(s). All playlists must use unique labels and names so you can distinguish between them.</p>

    <h2 class="label">Search</h2>

    <p>You can fuzzy search by playlist label using partial or full label.</p>

    <h2 class="label">Keyboard Shortcuts</h2>

    <ul>
      <li><kbd>CONTROL + n</kbd>: New playlist.</li>
      <li><kbd>CONTROL + s</kbd>: Save playlist (when within form).</li>
    </ul>
  <% end %>

  <% if playlists.any? %>
    <ul class="bit-gallery">
      <% playlists.each do |playlist| %>
        <%= playlist.render :playlist %>
      <% end %>
    </ul>

    <%= scope(:tooltip_action_content, label: "New").render %>
    <%= scope(:tooltip_action_content, label: "Search").render %>
    <%= scope(:tooltip_action_content, label: "Clear").render %>
    <%= scope(:tooltip_action_content, label: "View").render %>
    <%= scope(:tooltip_action_content, label: "Edit").render %>
    <%= scope(:tooltip_action_content, label: "Play").render %>
    <%= scope(:tooltip_action_content, label: "Clone").render %>
    <%= scope(:tooltip_action_content, label: "Mirror").render %>
    <%= scope(:tooltip_action_content, label: "Delete").render %>
  <% else %>
    <div class="bit-gallery">
      <%= render "shared/empty", message: "No playlists found." %>
    </div>
  <% end %>
</section>
